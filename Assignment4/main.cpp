#include "DblList.h"
#include "ListNode.h"
#include "ListQueue.h"
#include "IO.h"

int main() {
    /* Test for DoublyLinkedList.

    DoublyLinkedList<double> doubles;
    doubles.insertFront(1.1);
    doubles.insertFront(2.2);
    doubles.removeBack();
    doubles.insertFront(3.3);
    doubles.insertFront(4.4);
    doubles.insertFront(5.5);
    doubles.insertFront(6.6);
    doubles.remove(4.4);
    doubles.printList();

    */
    /*
    ListQueue<int> q;
    q.enqueue(1);
    q.enqueue(2);
    q.enqueue(3);
    q.enqueue(4);
    int temp = q.getSize();
    for(int i = 0; i < temp; i++) {
        std::cout << q.dequeue() << std::endl;
    }
    */

   IO input("inputFile.txt");

}